import { FaArrowRight, FaLeaf } from "react-icons/fa";
import { PageHeader } from "@/components/layout/page-header";
import { meals } from "@/lib/mock-data";
import { Button } from "@/components/ui/button";

export default function DietPage() { return <><PageHeader title="Your nutrition plan" description="Balanced meals built around performance and recovery." action={<Button>Generate new plan</Button>} /><section className="grid gap-6 xl:grid-cols-[1.5fr_1fr]"><div className="grid gap-4">{meals.map((item) => <article key={item.meal} className="panel flex items-center gap-4 p-5"><span className="grid h-12 w-12 place-items-center rounded-xl bg-emerald-500/10 text-emerald-500"><FaLeaf /></span><div className="flex-1"><p className="text-xs font-bold uppercase tracking-widest text-emerald-600">{item.meal}</p><h2 className="mt-1 font-bold">{item.name}</h2><p className="mt-1 text-sm text-slate-500">{item.calories} kcal · {item.protein}g protein</p></div><FaArrowRight className="text-slate-400" /></article>)}</div><aside className="panel bg-gradient-to-br from-slate-900 to-slate-800 p-6 text-white"><p className="text-sm text-emerald-400">Daily macro target</p><p className="mt-2 text-4xl font-bold">1,920 <span className="text-base font-medium text-slate-400">kcal</span></p><div className="mt-8 grid gap-4">{[["Protein", "132g", "bg-sky-400"], ["Carbs", "211g", "bg-emerald-400"], ["Fats", "59g", "bg-violet-400"]].map(([name, amount, color]) => <div key={name}><div className="mb-2 flex justify-between text-sm"><span>{name}</span><span>{amount}</span></div><div className="h-2 rounded-full bg-white/10"><div className={`h-2 rounded-full ${color}`} style={{ width: "72%" }} /></div></div>)}</div></aside></section></> }
